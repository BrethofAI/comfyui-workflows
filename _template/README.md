# `<workflow-name>`

> One-sentence description of what this workflow produces.

## What it does

2-3 paragraphs on the technique, the inputs, and the output. Include a
sample image / video URL if hosted externally.

## Required

- **ComfyUI commit:** `<sha>` — tested on this exact commit. Newer
  commits *probably* work; older may not.
- **Custom nodes:**
  - `<github-url>` @ `<commit-sha>`
  - ...
- **Model files:**
  - `<huggingface-url>` — `<filename>` — SHA256 `<sha>` — placed in
    `ComfyUI/models/<subdir>/<filename>`
  - ...
- **VRAM:** Minimum / recommended.

## How to run

1. Step one.
2. Step two.
3. Compare to `samples/sample-1.png`.

## Sample outputs

Inline links / images for `samples/sample-1.png`, etc.

## Known issues

- Anything brittle.

## License

Workflow JSON: MIT (this repo).
Models: see each model's own licence.
